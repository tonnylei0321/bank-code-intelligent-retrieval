# 问答界面模型选择功能

## 实现时间
2026-01-19

## 功能描述
在智能问答界面添加模型选择功能，允许用户选择特定的训练好的模型进行问答查询。

## 用户需求
> "这个窗口，我想应该有一个让我选择已经训练完毕的模型，然后针对指定的训练完毕的模型进行智能问答，否则我不知道后台用的具体是哪个模型"

## 实现内容

### 1. 前端界面改进

#### 新增功能
1. **模型选择下拉框**
   - 显示所有已完成训练的模型
   - 显示模型信息：Job ID、模型名称、Loss值、完成时间
   - 默认选择最新的模型
   - 实时显示当前使用的模型

2. **模型信息展示**
   - 模型列表自动加载
   - 显示训练任务ID
   - 显示基础模型名称（Qwen2.5-0.5B, Qwen2.5-1.5B, GPT-2等）
   - 显示训练Loss值
   - 显示完成时间

3. **用户提示**
   - 如果没有可用模型，提示用户先训练模型
   - 选择模型后显示确认信息
   - 查询时验证是否已选择模型

#### 界面布局
```
┌─────────────────────────────────────────────────────┐
│ 智能问答                                              │
├─────────────────────────────────────────────────────┤
│ ℹ️ 模型选择                                           │
│ 🤖 请选择要使用的训练模型：                            │
│ ┌───────────────────────────────────────────────┐   │
│ │ [Job #20] Qwen/Qwen2.5-1.5B (Loss: 0.1234)  │   │
│ │ [Job #19] Qwen/Qwen2.5-0.5B (Loss: 0.2345)  │   │
│ │ [Job #18] Qwen/Qwen2.5-0.5B (Loss: 0.3456)  │   │
│ │ [Job #16] gpt2 (Loss: 0.4567)                │   │
│ └───────────────────────────────────────────────┘   │
│ ✓ 当前使用模型：Qwen/Qwen2.5-1.5B                     │
├─────────────────────────────────────────────────────┤
│ RAG检索增强：[开启] 将使用知识库检索增强回答           │
│ ┌───────────────────────────────────────────────┐   │
│ │ 请输入您的问题...                              │   │
│ └───────────────────────────────────────────────┘   │
│                                          [提问]      │
└─────────────────────────────────────────────────────┘
```

### 2. 后端API改进

#### QueryRequest Schema扩展
```python
class QueryRequest(BaseModel):
    """Single query request"""
    question: str = Field(..., min_length=1, max_length=500, description="User's question")
    model_id: Optional[int] = Field(None, description="Training job ID to use for query")
    use_rag: bool = Field(True, description="Whether to use RAG")
    top_k: int = Field(5, ge=1, le=20, description="Number of documents to retrieve")
```

#### Query Endpoint改进
```python
@router.post("/", response_model=QueryResponse)
async def query_bank_code(
    request: QueryRequest,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    # 1. 如果指定了model_id，使用指定的模型
    if request.model_id:
        job = db.query(TrainingJob).filter(
            TrainingJob.id == request.model_id,
            TrainingJob.status == "completed"
        ).first()
        model_path = job.model_path
    
    # 2. 否则使用最新的模型
    else:
        latest_job = db.query(TrainingJob).filter(
            TrainingJob.status == "completed"
        ).order_by(TrainingJob.completed_at.desc()).first()
        model_path = latest_job.model_path
    
    # 3. 动态加载指定的模型
    query_service = QueryService(db=db)
    query_service.load_model(model_path)
    
    # 4. 执行查询
    response = query_service.query(question=request.question, ...)
    return response
```

### 3. 技术实现细节

#### 前端实现
**文件**: `frontend/src/pages/QAInterface.tsx`

**新增状态**:
```typescript
const [availableModels, setAvailableModels] = useState<TrainingJob[]>([]);
const [selectedModel, setSelectedModel] = useState<number | null>(null);
const [modelsLoading, setModelsLoading] = useState(false);
```

**模型加载**:
```typescript
const fetchAvailableModels = async () => {
  const response = await trainingAPI.getTrainingJobs();
  const completedJobs = response.data?.jobs.filter(
    job => job.status === 'completed' && job.model_path
  );
  setAvailableModels(completedJobs);
  
  // 默认选择最新的模型
  if (completedJobs.length > 0) {
    setSelectedModel(completedJobs[0].id);
  }
};
```

**查询提交**:
```typescript
const handleQuery = async () => {
  if (!selectedModel) {
    message.warning('请选择一个训练好的模型');
    return;
  }
  
  const response = await queryAPI.query({
    question: question.trim(),
    model_id: selectedModel,  // 传递选中的模型ID
    use_rag: useRAG,
    top_k: 5,
  });
};
```

#### 后端实现
**文件**: `mvp/app/api/query.py`

**动态模型加载**:
```python
# 根据model_id查询训练任务
job = db.query(TrainingJob).filter(
    TrainingJob.id == request.model_id,
    TrainingJob.status == "completed",
    TrainingJob.model_path.isnot(None)
).first()

# 创建新的query service实例
query_service = QueryService(db=db)

# 加载指定的模型（会自动从训练任务记录中读取正确的基础模型）
query_service.load_model(job.model_path)
```

**模型兼容性**:
- 自动从训练任务记录中读取基础模型名称
- 支持不同架构的模型（Qwen, GPT-2等）
- 支持不同尺寸的模型（0.5B, 1.5B, 3B等）

---

## 功能特性

### 1. 模型选择
- ✅ 显示所有已完成训练的模型
- ✅ 显示模型详细信息（ID、名称、Loss、时间）
- ✅ 默认选择最新模型
- ✅ 实时显示当前使用的模型

### 2. 智能验证
- ✅ 查询前验证是否选择了模型
- ✅ 验证模型是否存在且已完成训练
- ✅ 友好的错误提示

### 3. 动态加载
- ✅ 每次查询时动态加载指定的模型
- ✅ 自动适配不同的基础模型
- ✅ 支持多种模型架构

### 4. 用户体验
- ✅ 清晰的模型信息展示
- ✅ 直观的选择界面
- ✅ 实时反馈当前状态

---

## 使用流程

### 步骤1: 访问问答界面
1. 登录系统（admin/admin123）
2. 点击"智能问答"菜单

### 步骤2: 选择模型
1. 在页面顶部看到"模型选择"区域
2. 点击下拉框查看所有可用模型
3. 选择要使用的模型
4. 确认显示"✓ 当前使用模型：XXX"

### 步骤3: 进行查询
1. 输入问题
2. 设置RAG开关（可选）
3. 点击"提问"按钮
4. 查看答案和模型响应

### 步骤4: 切换模型
1. 随时可以切换到其他模型
2. 下次查询将使用新选择的模型
3. 可以对比不同模型的回答效果

---

## 模型信息展示

### 下拉框选项格式
```
[Job #20] Qwen/Qwen2.5-1.5B (Loss: 0.1234) 2026-01-19
[Job #19] Qwen/Qwen2.5-0.5B (Loss: 0.2345) 2026-01-19
[Job #18] Qwen/Qwen2.5-0.5B (Loss: 0.3456) 2026-01-19
[Job #16] gpt2 (Loss: 0.4567) 2026-01-19
```

### 信息说明
- **Job #XX**: 训练任务ID
- **模型名称**: 基础模型（Qwen/Qwen2.5-1.5B等）
- **Loss值**: 训练损失，越小越好
- **日期**: 训练完成时间

---

## API变更

### 请求格式（新）
```json
POST /api/v1/query
{
  "question": "中国工商银行北京分行的联行号是什么？",
  "model_id": 20,
  "use_rag": true,
  "top_k": 5
}
```

### 请求格式（旧）
```json
POST /api/v1/query
{
  "question": "中国工商银行北京分行的联行号是什么？"
}
```

### 兼容性
- ✅ 向后兼容：不指定model_id时自动使用最新模型
- ✅ 新参数可选：model_id, use_rag, top_k都是可选参数
- ✅ 默认行为：与之前保持一致

---

## 验证步骤

### 1. 检查模型列表
```bash
# 登录
TOKEN=$(curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin123" 2>/dev/null | \
  python3 -c "import sys, json; print(json.load(sys.stdin)['access_token'])")

# 获取训练任务列表
curl -X GET "http://localhost:8000/api/v1/training" \
  -H "Authorization: Bearer $TOKEN" | jq '.jobs[] | select(.status=="completed")'
```

### 2. 测试指定模型查询
```bash
# 使用Job 20（Qwen2.5-1.5B）
curl -X POST http://localhost:8000/api/v1/query \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "工商银行北京分行",
    "model_id": 20,
    "use_rag": true,
    "top_k": 5
  }'

# 使用Job 19（Qwen2.5-0.5B）
curl -X POST http://localhost:8000/api/v1/query \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "工商银行北京分行",
    "model_id": 19
  }'
```

### 3. 前端界面验证
1. 访问 http://localhost:3000
2. 登录后进入"智能问答"
3. 检查模型选择下拉框是否显示所有模型
4. 选择不同模型进行查询
5. 验证查询结果

---

## 技术优势

### 1. 灵活性
- 用户可以自由选择任何训练好的模型
- 支持对比不同模型的效果
- 可以测试不同基础模型的性能

### 2. 透明性
- 用户清楚知道使用的是哪个模型
- 显示模型的训练指标（Loss）
- 显示模型的训练时间

### 3. 可扩展性
- 支持添加更多模型信息
- 可以添加模型评分和推荐
- 可以添加模型对比功能

### 4. 用户体验
- 直观的界面设计
- 清晰的信息展示
- 友好的错误提示

---

## 后续优化建议

### 1. 模型推荐
```typescript
// 根据Loss值推荐最佳模型
const recommendedModel = availableModels.reduce((best, current) => 
  current.train_loss < best.train_loss ? current : best
);
```

### 2. 模型对比
```typescript
// 允许同时使用多个模型查询并对比结果
const compareModels = async (question: string, modelIds: number[]) => {
  const results = await Promise.all(
    modelIds.map(id => queryAPI.query({ question, model_id: id }))
  );
  return results;
};
```

### 3. 模型评分
```typescript
// 显示模型的用户评分和使用次数
interface ModelStats {
  model_id: number;
  usage_count: number;
  avg_rating: number;
  avg_response_time: number;
}
```

### 4. 模型标签
```typescript
// 为模型添加标签（推荐、最新、最快等）
<Tag color="gold">推荐</Tag>
<Tag color="green">最新</Tag>
<Tag color="blue">最快</Tag>
```

---

## 相关文件

### 前端
- `frontend/src/pages/QAInterface.tsx` - 问答界面主文件
- `frontend/src/services/api.ts` - API服务

### 后端
- `mvp/app/api/query.py` - 查询API端点
- `mvp/app/services/query_service.py` - 查询服务
- `mvp/app/models/training_job.py` - 训练任务模型

---

## 总结

✅ **功能已完成并可验证！**

**实现内容**:
1. 前端添加了模型选择下拉框
2. 显示所有已完成训练的模型信息
3. 后端支持指定model_id进行查询
4. 动态加载指定的模型
5. 自动适配不同的基础模型

**用户价值**:
1. 清楚知道使用的是哪个模型
2. 可以对比不同模型的效果
3. 可以选择最适合的模型
4. 提高了系统的透明度和可控性

**技术亮点**:
1. 动态模型加载
2. 自动基础模型适配
3. 向后兼容的API设计
4. 友好的用户界面

现在用户可以在问答界面自由选择要使用的模型，并清楚地知道当前使用的是哪个模型！
